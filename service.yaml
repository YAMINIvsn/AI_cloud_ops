apiVersion: v1
kind: Service

metadata:
  name: cloudops-service

spec:
  type: NodePort

  selector:
    app: cloudops-ai

  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080